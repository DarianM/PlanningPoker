import React from "react";

const UserNameLog = props => (
  <div id="user">
    <h4>User:</h4>
    <input id="userName" className="sessionId" type="text" placeholder="Name" />
  </div>
);

export default UserNameLog;
